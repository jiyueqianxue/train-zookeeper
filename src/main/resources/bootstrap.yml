spring:
  application:
    name: train-data
  cloud:
    config:
      uri: http://msc.traininfo.com:80
      label: ${DEPLOY_ENV}
  velocity: 
    suffix: .vm
multipart:
  maxFileSize: 50Mb
  maxRequestSize: 50Mb
